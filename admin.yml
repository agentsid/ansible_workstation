---
- name: Admin playbook (in develop)
  hosts: all
  gather_facts: true
  become: yes
  vars_files:
    - secret.yml
  tasks:
    - name: Block 1 
      block:
      - name: <-> 
        debug:
          msg: "Engineer playbook"
      - name: >
          checking root user
        shell: whoami
        register: stdout
        no_log: true
      - debug: msg="{{stdout.stdout}}"
    


    - name: Include tasks
      include_role: 
        name: "{{role}}"
      