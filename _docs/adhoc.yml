ansible-navigator inventory -m stdout --list

# v8.4
#adhoc commands

ansible -m setup localhost | grep ansible_python_version


ansible washington1.example.com --list-hosts

# this will by defulat use /etc/ansible/hosts 
ansible all --list-hosts

ansible ungrouped --list-hosts

ansible all -i <file_name> --list-hosts

ansible all -i inventory/hosts  --list-hosts

sudo ssh-copy-id -i ~/.ssh/id_rsa.pub ansible@172.17.0.3

ansible-playbook admin.yml --extra-vars " role=shell"

# docker commands
docker build -f dockerfile -t="dockerfile/ubuntu-img" .

ansible_workstation$ ansible-playbook  administrator.yml  -e  "role='am' task='ping' hosts='d_host'"

# mysql container
docker run --name new_mysql -p 3306:3306 -e MYSQL_ROOT_PASSWORD=ready2go -d mysql:latest
docker exec -it mysql bash
docker exec -it  mysql -uroot -pready2go
mysql -uroot -p
docker inspect mysql --format='{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}'