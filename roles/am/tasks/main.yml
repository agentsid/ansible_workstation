---

- name: >
     Executing Anisble Module "{{task}}"
  include_tasks:
    file: "{{items|join('')}}"
  vars:
    items:
      - "{{(task == 'ping')|ternary('ping.yml,'')}}"
      - "{{(task == 'shell')|ternary('shell.yml,'')}}"