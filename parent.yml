---
- name: Parent playbook (Administarator)
  hosts: all
  gather_facts: true
  become: yes

  tasks:
    - name: Block1
      block:
        - name: Include builtin Ansible role
          ansible.builtin.include_role:
            name: shell
        

# - name: Update db servers
#   hosts: databases
#   remote_user: root

#   tasks:
#   - name: Ensure postgresql is at the latest version
#     ansible.builtin.yum:
#       name: postgresql
#       state: latest
#   - name: Ensure that postgresql is started
#     ansible.builtin.service:
#       name: postgresql
#       state: started